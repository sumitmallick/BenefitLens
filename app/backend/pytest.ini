[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=claims --cov-report=term-missing
markers =
    unit: pure domain logic, no I/O
    integration: requires running PostgreSQL
    slow: known slow tests
