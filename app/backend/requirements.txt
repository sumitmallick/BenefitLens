# Core
fastapi==0.115.5
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1          # passlib 1.7.4 is incompatible with bcrypt>=5.x
python-multipart>=0.0.9  # required for OAuth2 form data
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10   # for alembic sync migrations

# PHI encryption
cryptography==43.0.3

# Redis (caching)
redis==5.2.1
hiredis==3.0.0

# Kafka (event publishing)
aiokafka==0.11.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2           # async test client for FastAPI
factory-boy==3.3.1
faker==33.1.0

# Linting / formatting (dev)
ruff==0.8.4
mypy==1.13.0

# Rate limiting (Redis-backed distributed throttling)
slowapi==0.1.9

# Observability
structlog==24.4.0
prometheus-fastapi-instrumentator==7.0.0
