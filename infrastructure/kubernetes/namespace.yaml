apiVersion: v1
kind: Namespace
metadata:
  name: claims
  labels:
    app.kubernetes.io/part-of: claims-processing
    app.kubernetes.io/managed-by: kubectl
    # Required by NetworkPolicy namespaceSelector rules that reference this namespace
    kubernetes.io/metadata.name: claims
  annotations:
    # Prevent accidental deletion of the namespace (requires explicit --force)
    kubectl.kubernetes.io/last-applied-configuration: ""
